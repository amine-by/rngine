import GameEngine from './GameEngine';
import {
  configure,
  pause,
  resume,
  spawn,
  despawn,
  update,
} from './GameMethods';

export { GameEngine, configure, pause, resume, spawn, despawn, update };
