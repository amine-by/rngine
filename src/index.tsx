import GameEngine from './GameEngine';
import {
  initialize,
  pause,
  resume,
  spawn,
  despawn,
  setP,
  setV,
} from './GameMethods';

export { GameEngine, initialize, pause, resume, spawn, despawn, setP, setV };
